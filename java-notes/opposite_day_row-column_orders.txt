OPPOSITE DAY


row major order:


	for (int a = 0; a < matrix.length; a++) {
		student1 = 0;

		for (int b = 0; b < matrix[a].length; b++) {

			student1 = matrix[0][a];  //goes to row one and prints it
/*
for second row, matrix[1][a]
for third row, matrix[2][a]
for last row, matrix[3][a]

 */
				System.out.println("student 1's results for quiz " + (a+1) +" are: " + student1);

		break;
   }//row

}//column










column major order:


//termination condition is length of column
	for (int a = 0; a < matrix[0].length; a++) {  //in becomes out so matrix[0].length replaces matrix.length 
	
	   quiz = 0;
	   
//termination condition is length of row
		for (int b = 0; b < matrix.length; b++) { //out becomes in so matrix.length replaces matrix[0].length 

			quiz = matrix[a][0];                 //go to row and print first  == column (0)

   }//row
			System.out.println("student " + (a+1) + "'s results for first quiz: " + quiz); //a+1 so that it prints student 1 instead of student 0
  }//column